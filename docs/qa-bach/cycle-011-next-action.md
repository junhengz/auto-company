# Cycle 011: Next Action (QA-Bach)

**Next Action:** merge PR #2 and PR #3 into `nicepkg/auto-company:main`, then set `HOSTED_WORKFLOW_BASE_URL_CANDIDATES` to 2-4 real workflow-runtime origins (must pass `GET /api/workflow/env-health`) and run a manual dispatch of `cycle-005-hosted-persistence-evidence` with `preflight_only=true`.

