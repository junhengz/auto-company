# Next Action (Cycle 011)

Get 2-4 real deployed workflow-runtime origins (the Next.js app that serves `GET /api/workflow/env-health`), set `HOSTED_WORKFLOW_BASE_URL_CANDIDATES` on `junhengz/auto-company`, then rerun `cycle-005-hosted-persistence-evidence` with `preflight_only=true`.

